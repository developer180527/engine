$input a_position
#include <bgfx_shader.sh>
void main() {
    mat4 model  = u_model[0];
    gl_Position = mul(u_viewProj, mul(model, vec4(a_position, 1.0)));
}

// TODO (Jun 4, 09:00 PM):
// Support cascaded shadow maps for directional lights.
// Support point-light shadow cubemaps.
// Add alpha-tested shadow caster support.
// Add skinned mesh shadow casting.