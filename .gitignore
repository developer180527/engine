build/
build-asan/
build-ship/

.cache/
.vscode/
.idea/
*.dSYM/
compile_commands.json
.DS_Store
docs/generated
/assets/
# Kits (separate repos) + user game projects — not part of the engine repo
Kits/
fps_shooter/

# Compiled tool binaries (sources live in tools/)
fbx_diag
# ImGui window layout, rewritten on every panel move
imgui.ini
# macOS custom-folder-icon file; its name ends in a carriage return, so the
# `?` is a single-character wildcard rather than punctuation.
Icon?

# Linux container build tree + ccache (docker/linux-build.Dockerfile)
build-linux/
.ccache-linux/
