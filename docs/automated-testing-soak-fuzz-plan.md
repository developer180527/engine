# Automated Engine Testing Suite — Seeded Fuzzing, Soak Farm & Profiling

> **STATUS (2026-07-29).** Partially built, and the rollout order at the bottom
> of this document was **deliberately inverted** — read this box first.
>
> **Built and gating:** `tests/fuzz/fuzz.h` implements §1.1 seed splitting
> (`deriveSeed(master, domain)`) and the §1.3 `ReproKey` including the
> generator-version pin. Two targets exist — `fuzz_ddc_manifest_test` and
> `fuzz_mesh_loader_test` — with a committed seed corpus under
> `tests/fuzz/corpus/`. Two ctest lanes: **`fuzz-regress`** replays the whole
> corpus (fast, deterministic, runs in CI on every change — this is the
> mechanism that makes "must pass all previous tests" true) and
> **`fuzz-explore`** runs fresh seeds (long, nightly, feeds the corpus). CI now
> also runs the `unit` lane, which it previously never did at all.
>
> **NOT built:** `EngineTestHarness`, `TraceRecorder`, `TraceReplayer`, and the
> `engine-test-infrastructure-plan.md` referenced below — *that document does
> not exist in this repo*. So §1.4 (artifact capture), all of Part 2 (soak
> farm), and the input-bot / kit-churn / scene-spawn / timing-jitter domains of
> §1.2 are still design only.
>
> **Why the order changed:** the rollout list starts with input-bot and
> kit-churn, which are precisely the domains that need the unbuilt whole-engine
> harness — following it literally means building weeks of scaffolding before
> the first bug. Starting instead with harness-free targets (pure deserializers
> and self-contained state machines) found **three real bugs within minutes of
> the first run**: `ddcFetchRecord` returning success for a manifest with no
> primary member, and two in `assetlib::loadMesh` — unbounded allocation
> straight from untrusted header counts (a 4.5 TB `resize` from
> `materialCount`) and truncation accepted as success via
> `return f.good() || f.eof()`. Build the whole-engine harness when the
> whole-engine domains are actually wanted; it is a separate project, not a
> prerequisite for useful fuzzing.

This builds directly on `engine-test-infrastructure-plan.md` — `EngineTestHarness`, `TraceRecorder`/`TraceReplayer`, and the layered tier strategy are assumed to exist. This plan adds the three things asked for on top of that foundation: **reproducible-by-seed fuzzing**, **a self-hosted long-running test farm**, and **one-click, auto-discovering automation** — plus the profiling needed to make days-long runs actually tell you something.

---

## Part 1 — Seeded, reproducible fuzzing

The requirement "a certain fuzzy test could be replicated or regenerated by the seed" means one thing architecturally: **every source of randomness anywhere in a test run must trace back to one master seed**, with no hidden entropy (real `std::random_device`, wall-clock-seeded RNGs, OS thread-scheduling-dependent behavior affecting logical outcomes) sneaking in. If any part of the run pulls unseeded randomness, "replicate by seed" quietly becomes a lie the first time it matters.

### 1.1 — Seed splitting, not one shared RNG

A single shared PRNG passed around by reference causes a subtle, common bug: consuming a random number for one purpose (say, bot input generation) shifts the sequence for everything downstream (scene fuzzing, timing jitter), so adding one new consumer of randomness silently changes every other domain's output for a given seed. Instead:

```cpp
// One masterSeed per test run. Every domain gets its own independent stream,
// derived deterministically — same masterSeed always yields the same substream
// for a given domain, regardless of what else consumes randomness that run.
uint64_t deriveSeed(uint64_t masterSeed, const char* domain) {
    return splitmix64(masterSeed ^ fnv1a64(domain));
}

FuzzRng inputBotRng   = FuzzRng(deriveSeed(masterSeed, "input_bot"));
FuzzRng sceneSpawnRng = FuzzRng(deriveSeed(masterSeed, "scene_spawn"));
FuzzRng timingJitterRng = FuzzRng(deriveSeed(masterSeed, "timing_jitter"));
FuzzRng assetCorruptRng = FuzzRng(deriveSeed(masterSeed, "asset_corrupt"));
FuzzRng kitChurnRng   = FuzzRng(deriveSeed(masterSeed, "kit_churn"));
```

Adding a new fuzz domain later (a new subsystem) is just one more `deriveSeed(masterSeed, "new_domain")` call — it never perturbs existing domains' output. This is the mechanism that keeps old repro keys valid forever, even as the fuzz surface grows.

### 1.2 — The five fuzz domains to build, in priority order (matches confirmed bugs)

| Domain | What it randomizes | Bug classes it targets |
|---|---|---|
| **Input bot** | Semi-random key/button presses, mouse motion, timing, within a configurable "plausible player" distribution | Sub-tick edge overflow (L.4), hybrid raw/window handoff (L.5), action/context stack edge cases |
| **Asset corruption** | Bit-flips / truncation / structural mutation of cooked assets and `input.json` before load | Unguarded `stof`/`stoi` (H.4), AsyncLoader parser robustness, Assimp/gltf ingestion crashes |
| **Kit churn** | Random load/unload/reload ordering and timing, including concurrent triggers | `KitHost`/`ModuleLibrary` dead-entry-on-failed-reload (C.2), dependency-cycle ordering |
| **Scene/entity spawn** | Random entity counts, component combinations, parent/child depth, within valid schema bounds | ECS archetype stress, `safeReparent`'s cycle/depth guard, `EventSweeper` under heavy churn |
| **Timing jitter** | Randomized `dt` sequences, simulated frame hitches, simulated hotplug timing | `m_simAccumulator` cross-session leak (H.5), HID `devMu` contention (M.2), job scheduling races |

Each domain is a small, independent generator function: `(FuzzRng&, int stepIndex) -> Action`, called once per simulated tick from the orchestrator, feeding into `EngineTestHarness`. Because it's driven through the harness's existing manual-stepping interface, no new engine-side hooks are needed beyond what Phase 1 of the prior plan already specifies.

### 1.3 — The repro key: what gets captured on any failure

```cpp
struct ReproKey {
    uint64_t    masterSeed;
    std::string engineCommitHash;
    std::string testSuiteVersion;      // the fuzz generators themselves version too
    std::string scenarioConfig;        // which project/scene, which domains active
    uint32_t    failedAtTick;          // where it broke, for fast re-run without replaying to the end
};
```

`testSuiteVersion` matters as much as the engine's own commit: if the *fuzz generator* changes (e.g. someone widens the input-bot's random key range), the exact same `masterSeed` produces different bot behavior. A repro key must pin both, or "replicate by seed" quietly breaks the day someone improves the fuzzer. `--replay <repro-key>` reconstructs the engine build (via commit hash) and re-derives every substream deterministically — a bug found on day 3 of an unattended run is fully reproducible on a laptop with one command.

### 1.4 — Dual capture: seed *and* artifact

Regenerating from a seed is cheap to store but assumes the fuzz generator's logic never changes in a way that breaks old seeds' meaning (see above) — and assumes no OS/library-level nondeterminism leaks in (thread scheduling affecting *logical*, not just timing, outcomes, would be its own bug worth finding separately). Belt-and-suspenders: **every fuzz run also persists its generated input stream as a real `TraceRecorder` artifact** (from the prior plan), not just the seed. Seed-regeneration is the primary, cheap repro path; the archived artifact is the fallback that works even if the generator changes later, and it's also directly useful for the trace inspector (Phase 4 of the prior plan) without needing to re-run anything.

---

## Part 2 — The soak farm: running for days on self-hosted hardware

### 2.1 — Orchestrator architecture

A daemon (call it `soakd`) on your self-hosted machine(s), whose only job is scheduling **campaigns** and capturing what happens. A campaign is:

```
Campaign {
    build:        commit hash + build config (debug+ASan+UBSan / release / TSan)
    suite:        which fuzz domains + which golden-replay set + which stress scenarios
    seedPolicy:   "fresh" (new random seeds) | "targeted" (re-run seeds that found bugs
                  before, with small mutations — cheap coverage-guided-ish fuzzing)
    duration:     fixed (e.g. 4 hours) | until-stopped (the multi-day soak)
}
```

### 2.2 — What actually runs, in rotation

Not everything should run in the same mode for days — mix short and long:

- **Golden-replay regression** (from the prior plan) — fast, runs every commit, gates merges. Not really part of the soak farm's job, but shares the same harness/build infra.
- **Seeded fuzz bursts** — short (minutes), high-volume, many seeds per hour, across all five domains, rotated through sanitizer builds (ASan+UBSan one rotation, TSan the next — they can't combine, per the compiler-sanitizer plan from earlier). This is where most crash-class bugs (like the ones already found: C.1–C.5) get caught fast, because volume matters more than duration for this category.
- **Long soaks** — one real `EngineTestHarness` instance, headless, left running for the full multi-day window, driven by a *low-intensity* combination of the fuzz domains (steady but modest input bot activity, occasional kit churn, occasional scene load/unload) — intentionally gentler than the crash-hunting bursts, because the goal here is different: catching slow accumulation (`ScriptHost` observer leak under repeated Play/Stop-equivalent cycles, `m_lookTotalX` double-precision drift, `MemoryChannel` growth trends, kit-reload graveyard/temp-file accumulation) that only shows up over sustained real time, not sheer event volume.
- **Stress bursts** — short, deliberately extreme (ring buffer overflow scenarios, sub-tick edge overflow, many-kit dependency graphs with cycles) — scheduled periodically, not continuously, since they're about hitting a specific boundary condition, not about duration.

### 2.3 — Anomaly detection and capture

Any of the following triggers immediate capture, not just a log line:
- Process crash / signal (SIGSEGV, SIGABRT, etc.)
- Sanitizer trip (ASan/UBSan/TSan report)
- Assertion failure
- **Watchdog timeout** — a campaign's harness should have a per-tick or per-frame deadline; if `stepFrame()` doesn't return within some generous bound, treat it as a hang, not silence. This matters specifically because of things like the `devMu` contention issue (M.2) or a `wait()` on a stale `JobHandle` (C.5) — both could manifest as a hang rather than a crash.
- **Memory trend breach** — not a single-frame event, but `soakd` watching `MemoryChannel`'s persisted time series (see Part 3) and flagging when live bytes cross a threshold-over-time slope, ending that soak leg early so the failure state is captured while still fresh rather than after an OOM kill destroys the evidence.

On any trigger: freeze and archive the `ReproKey`, the full `TraceRecorder` artifacts generated so far in that run, the last N minutes of every active profiler channel's time series, and — where feasible — a core dump / sanitizer report.

### 2.4 — Crash minimization (stretch, high value once the basics work)

Once a fuzz run finds a failing seed, a separate minimization pass (run once per unique failure, not continuously) does delta-debugging: replay with progressively fewer steps / a narrower domain subset, binary-searching for the shortest sequence that still reproduces. This turns "it crashed somewhere in a 6-hour run" into "steps 40–47 of the kit-churn domain reproduce it in 200ms" — dramatically cheaper to actually debug. This is standard practice in mature fuzzing infrastructure (AFL/libFuzzer both do this); worth building once Part 1 and 2.1–2.3 are solid, not before.

### 2.5 — Deduplication

Long fuzz campaigns rediscover the same bug repeatedly unless failures are deduplicated — hash the crash signature (stack trace top N frames, or sanitizer report's key line) and only surface genuinely new signatures in the report; track recurrence count for known ones instead of drowning the dashboard in duplicates of C.1 every night.

---

## Part 3 — Profiling: making days of runtime actually legible

Point sampling ("what does memory look like right now") is what the engine's profiler channels already give you (`MemoryChannel`, `InputLatencyChannel`). For a multi-day run, you need **persisted time series**, not just last-frame console prints.

### 3.1 — Persist every channel, not just print it

Minimal change to the existing channel framework: add an optional sink to `prof::IProfilerChannel` that appends each frame's (or each N-th frame's, to control volume) stats to a structured, timestamped log — a simple flat binary or CSV format, one row per sample. `MemoryChannel` and `InputLatencyChannel` already compute exactly the right numbers; this only adds "and also write them down" rather than only printing to stdout.

### 3.2 — Trend detection, not just raw numbers

A multi-day soak generates far too many samples to eyeball. Build a small analysis pass (can be part of `trace_inspect` from the prior plan, or a sibling tool) that:
- Runs a simple linear regression on live-bytes-over-time per memory tag; a sustained positive slope with no plateau across hours is a leak candidate — this is exactly the shape the `ScriptHost` observer-accumulation bug (C.1) would produce if it weren't already fixed, and a mechanism like this is what would have caught it *before* someone traced it by reading code.
- Tracks p50/p95/p99 for `InputLatencyChannel`'s queue/tick/look-lag numbers over the run, flagging any upward drift — this is the number that would catch `devMu` contention (M.2) manifesting as gradually worsening tail latency rather than a hard failure.
- Tracks job-scheduling latency (time between `run()` and completion) similarly, once `jobs` gets a profiler channel of its own — currently it doesn't have one; worth adding given `jobs_enkits.cpp` is exactly the kind of subsystem the earlier plan's checklist would flag as needing one (allocates, blocks, has a latency-sensitive path).

### 3.3 — Low-overhead always-on sampling for CPU

For "did this get slower" questions over a multi-day window, an instrumenting profiler is too much overhead to leave on continuously. Use periodic stack sampling instead (`perf record` at a low frequency on Linux, or the platform equivalent) left running for the whole soak, correlated against the same timestamp axis as the memory/latency time series — so a "things got slower around hour 30" finding from Part 3.2 can be cross-referenced against what the sampling profiler was actually spending time in at that point, without needing to reproduce the slowdown separately.

### 3.4 — Correlate on failure

When Part 2.3 captures an anomaly, it should pull the same time window from all of Part 3's persisted data — memory trend, latency percentiles, CPU sampling — into one bundle alongside the repro key and trace artifacts. The goal is that opening one failure record answers "what was the seed, what was happening in the engine, and what was the system doing" without needing three separate manual lookups.

---

## Part 4 — One-click, auto-discovering automation

### 4.1 — Registration, not manual wiring

The mechanism that keeps this reusable as new subsystems get added: a link-time self-registration macro, the same pattern the engine likely already uses for plugins (`PluginRegistry::add()`):

```cpp
// In a new subsystem's test file — this is the ENTIRE integration step
// required to get picked up by every campaign type automatically.
ENGINE_REGISTER_FUZZ_DOMAIN("job_scheduling", jobSchedulingFuzzStep);
ENGINE_REGISTER_GOLDEN_FIXTURE("kit_host_reload_sequence", kitHostGoldenTest);
ENGINE_REGISTER_TIER1_TEST("event_sweeper_mark_sweep", eventSweeperTest);
```

`soakd` and the CI golden-replay lane both enumerate these registries at startup rather than needing per-subsystem orchestrator code. Adding a new subsystem to the *entire* testing pipeline — fuzzing, soaking, golden regression, CI — becomes "add these macro calls in the subsystem's own test file," matching the checklist from the prior plan's Phase 5, but now mechanically enforced rather than a checklist item someone can skip.

### 4.2 — The one-click entry point

A single top-level command:
```
./run_test_campaign.sh --duration 72h --seeds fresh --report ./reports/2026-07-15/
```
does, in order: builds the current commit across the needed configurations (ASan+UBSan, TSan, release), auto-discovers every registered fuzz domain / golden fixture / Tier 1 test via 4.1's registries, hands them to `soakd` for the requested duration, and — regardless of how the run ends (completed, or stopped early by an anomaly) — produces the Part 3/Part 2 combined report at `--report`'s path. No step in this requires editing orchestrator code when a new subsystem is added; only the new subsystem's own test file needs the registration macros.

---

## Rollout order

1. **Seed-splitting + `ReproKey` infrastructure** (1.1–1.3) — cheap, foundational, everything else depends on repro being trustworthy from day one.
2. **Two fuzz domains to start**: input bot and kit churn (1.2) — these map directly to bugs already found (sub-tick edges, `KitHost` reload) so they pay for themselves immediately as regression coverage, not just future-proofing.
3. **`soakd` basic orchestrator** (2.1–2.3) without minimization or dedup yet — get campaigns running and anomalies captured first.
4. **Persisted profiler time series** (3.1) — needed before soaks are worth running for days, since otherwise a multi-day run just produces console spam nobody reads.
5. **Registration macros + one-click script** (Part 4) — do this once there are at least two real fuzz domains and one golden fixture to register, so the convention is validated against real use rather than designed in the abstract.
6. **Trend detection, crash minimization, dedup** (2.4, 2.5, 3.2) — genuine value-adds, but each is a refinement on top of a working pipeline, not a blocker to getting the pipeline running.

Want me to draft the actual seed-splitting utility and the `ENGINE_REGISTER_FUZZ_DOMAIN` macro next, against real C++ that would drop into this codebase?
